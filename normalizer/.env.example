# Copy to .env and fill in. NEVER commit .env.
#
# The normalizer is currently a generic skeleton — it reads no environment
# variables. This file is a placeholder; provider/model config gets added back
# once the official Problem Statement is published and the real classifier is
# wired in.